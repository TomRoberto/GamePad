import "./header.scss";

const Header = () => {
  return (
    <div>
      <p>GAMEPAD</p>
      <div>
        <button>My Collection</button>
        <button>Login</button>
      </div>
    </div>
  );
};

export default Header;
